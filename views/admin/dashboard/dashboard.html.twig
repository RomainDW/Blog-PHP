{% extends "base-admin.html.twig" %}

{% block title %}
    Dashboard
{% endblock %}

{% block stylesheet %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.19/css/dataTables.bootstrap4.min.css"/>
{% endblock %}

{% block main %}
    <main class="container" style="margin-top:160px;">
        {% if message.success %}
            <div class="alert alert-success" role="alert">
                {{ message.success }}
            </div>
        {% endif %}
        {% if message.error %}
            <div class="alert alert-danger" role="alert">
                {{ message.error }}
            </div>
        {% endif %}

        <!-- table blog posts -->
        <table id="list-posts" class="table table-striped table-bordered">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
            </thead>

            <tbody>
            {% for post in posts %}
                <tr>
                    <td>{{ post.title }}</td>
                    <td>{{ post.date_add|date("d/m/Y") }}</td>
                    <td>
                        <a href="?c=blog&t=edit&id={{ post.id }}" role="button" class="btn btn-primary">Modifier</a>
                        <a href="#" role="button" class="btn btn-danger">Supprimer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </main>
{% endblock %}

{% block js %}
    <script type="text/javascript" src="https://cdn.datatables.net/v/bs4/dt-1.10.18/datatables.min.js"></script>
    <script type="text/javascript" src="https://cdn.datatables.net/plug-ins/1.10.19/i18n/French.json"></script>
    <script src="assets/js/vendor/dataTables.js"></script>
{% endblock %}